import 'package:flutter/material.dart';

class AllDevicePage extends StatelessWidget {
  const AllDevicePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pagina all Devices'),
      ),
    );
  }
}
